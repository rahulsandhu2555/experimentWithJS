//Styles =>
    // 1. CSS Stylesheets  => import './stylesheet.css' => available to all the childs, not sugested if not common style
    // 2. inline styling =>
            //const headingStyle = { fontSize: 72px}
            //<h1 style ={headingStyle} > hi </h1>
    // 3. css module
            //import styles from './styleshee.module.css';
            //<h1 className={styles.headingStyle}>Hi</h1>
    // 4. css in JS libraries