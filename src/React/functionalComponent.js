//Functional Component
    //stateless without hooks
    //absence of this

const Greet = ({name, message}) => { //to destructure in the parameter
    //const { name, message } = props; //or like this props may contain 10 but extracting needed ones
 function clickHandler(){

 }
 return(
     <>hi</>
 )
}