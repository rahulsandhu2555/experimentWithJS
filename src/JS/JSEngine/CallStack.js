//when javascripts runs for the first time
    // two phase => creation, execution
        //creation => variable hoisting, assigning heap memory to ref type object function
        //execution => assigns value and executes line by line